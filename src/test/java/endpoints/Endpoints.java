package endpoints;

public class Endpoints {

    public static final String LOGIN = "/login",
            UNKNOWN = "/unknown/",
            USERS = "/users/",
            USERS_PAGE = "/users?page=";
}
